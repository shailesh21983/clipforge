"use client";import {useState} from "react";import {Header,Footer} from "../components";export default function Page(){const [topic,setTopic]=useState("");const [out,setOut]=useState([]);function run(){if(!topic)return;setOut([`Nobody is talking about ${topic}...`,`Stop doing ${topic} the hard way.`,`I wish I knew this about ${topic} earlier.`,`The truth about ${topic} might surprise you.`,`Here are 3 things I learned about ${topic}.`,`Most people get ${topic} completely wrong.`,`Try this before you give up on ${topic}.`,`This changed how I think about ${topic}.`,`A simple trick that makes ${topic} easier.`,`Don't scroll if you're interested in ${topic}.`])}return <><Header/><main className="wrap toolpage"><h1>Viral Hook Generator</h1><p className="muted">Generate short-form hooks for your next post or video.</p><div className="formgrid"><div className="field"><label>What is your content about?</label><input value={topic} onChange={e=>setTopic(e.target.value)} placeholder="e.g. Best AI tools for small businesses"/></div><button className="btn" onClick={run}>🔥 Generate Hooks</button></div>{out.length>0&&<div className="output">{out.map((x,i)=><div className="result" key={i}>{i+1}. {x}</div>)}</div>}</main><Footer/></>}